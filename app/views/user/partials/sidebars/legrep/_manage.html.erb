<h5 class="mb-3 mb-md-0 me-md-auto text-info">CONSOLE DE GESTION</h5>
<hr>

<h6 class="text-light text-end">Organisations gérées</h6>

<ul class="nav nav-pills flex-column mb-auto">
  <% current_user.managed_organizations.each do |organization| %>
    <li class="nav-item" style="font-size:smaller">
        <%= link_to "#{organization.name}", user_dashboards_organizations_legalreps_path(organization_managed: organization.id), class:"sidebar-item text-secondary text-decoration-none" %>
    </li>
  <% end %>
</ul>

<hr>