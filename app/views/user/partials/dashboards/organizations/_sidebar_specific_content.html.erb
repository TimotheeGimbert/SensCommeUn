  <% links = [] %>
  <% ActivitySector.all.each { |sector| links.push( {id:sector.id, label:sector.name} ) } %>

  <h4 class="text-warning">Secteurs d'activité</h4>
  
  <ul class="nav nav-pills flex-column mb-auto">
      <% links.each do |link| %>
        <li class="nav-item" style="font-size:smaller">
          <%= link_to "#{link[:label]}", user_dashboards_organizations_path(sector: link[:id]), class:"sidebar-item fs-6" %>
        </li>
      <% end %>
  </ul>

  <hr>